pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Ajoutez ici les commandes pour builder votre projet
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Ajoutez ici les commandes pour exécuter les tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
                // Ajoutez ici les commandes pour déployer votre projet
            }
        }
    }
}
